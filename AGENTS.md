# 项目工作规范

## 代码设计

- 强类型建模：使用领域类型表达有效状态和可预期的运行时失败，避免以字符串、布尔值、字典或重复字段维持隐含约定；让不可能状态在类型上不可构造，而非交由后续流程反复检查。
- 结构化目录：按一起变化的业务能力组织模块、目录与文件；简单能力保持扁平，只有存在独立职责时才建立 Domain、Application、Persistence、Platform、Presentation 等层级，不预建空目录。
- 高内聚拆分：不同模块分文件组织；同一模块内部，按独立职责和共同变化关系拆分文件。简单且紧密相关的实现保持在一起，复杂或可独立理解的职责主动拆开，不以类型数量或文件长度机械决定拆分。
- 入口边界：CLI 与 Obsidian 入口复用业务能力，核心用例不依赖命令行解析、进程退出或 Obsidian 界面对象。
- 副作用隔离：纯业务规则与副作用实现必须分属不同模块、使用不同文件。纯函数只接收不可变事实并返回结果，不访问文件系统、网络、时钟或随机数；应用层负责协调读取、规则计算与执行。
- 不滥用防御性编程：只对实际运行中可能发生的失败进行必要处理；对仅由实现错误产生的不可能状态，严格禁止静默兜底，也严格禁止保留旧版本、旧实现的兼容路径。

## Obsidian 集成与界面

- 原生优先：遵循 Obsidian 公开 API、原生组件（例如列表）及常见插件惯例，仅在无法满足需求时自定义。
- 界面简洁：仅保留操作所需文字，补充说明使用原生延迟悬停提示；完成反馈使用短暂通知，错误直接显示。
- 界面稳定：状态变化局部更新，保留滚动、展开和焦点；关联设置成组呈现，同排文字和控件垂直居中、统一对齐。
- 配置存储：使用原生接口读写插件目录的 `data.json`。

## 项目文档

- 需求与技术文档：spec/Requirements/ 记录产品行为与验收边界，不记录具体视觉细节选择，例如视觉参数/图标名称；spec/Technical/ 为每个业务能力维护技术说明。简单能力用一页说明职责、流程、关键源码及失败行为即可，流程图和独立专题按复杂度设置。数据所有权、外部 API 输入/输出、验证证据及设计依据应有唯一且可导航的位置，局部算法和完整类型定义由源码与测试表达。
- 渐进展开的文档编写模式：从 spec/ 及其各级子目录的 `Main.md` 开始阅读；它只保留总述、关键结论和导航。只有一个主题时直接使用主题文档，避免只转发单个链接的目录和导航页。
- 文档语言的马尔可夫性/禁止负向回声：以最新确认的要求作为唯一基线，从最终实现重新生成成品；成品面向未见过本次会话的读者，只陈述当前成立的事实；会话中的旧方案、纠正和中间尝试只作为修改控制信息，而不是正文素材。
- 禁止近因偏差：按整体重要性安排主次与篇幅长度，**禁止因近期修改放大局部细节**！局部变更应融入整体叙述并保持主次比例。尤其是 `AGENTS.md`, `README.md` 这些关键总览性文档。
- 证据与维护：区分官方接口约定、实际观察和推断；明确未验证范围。

## 开发规范

- 目录生命周期：`dist/` 为编译输出，`.artifacts/` 按用途和运行时间组织开发产物，`TestVaults/` 为测试用的 Obsidian vault 环境；分类与清理约定见[开发产物](spec/Technical/DevelopmentArtifacts.md)。
- 开发验证：插件变更通过检查后，将构建同步到 TestVaults/Debug-Vault/ 并重新加载验证

## Git 操作

- 写操作授权：本地写操作（暂存、提交、改写历史等）与远程写操作（推送、仓库变更、发布等）默认禁止，须分别取得用户明确授权，互不包含。
- 授权范围：临时授权仅覆盖当前一轮请求中明确指定的操作，至本轮最终回复失效，不跨轮次继承或自行扩大范围。
- commit message 的长短和修改的大小要匹配，小修改少写，大修改多写。